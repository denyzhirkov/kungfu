+++
id = "mem_0020"
kind = "decision"
title = "Provenance везде + MCP menu slimming 40→38 (эпик 6, v2.5.17) — must-have план закрыт"
tags = ["provenance", "mcp", "slimming", "honesty", "milestone"]
related_files = ["crates/kungfu-mcp/src/lib.rs", "crates/kungfu-mcp/src/tools/graph.rs", "crates/kungfu-mcp/src/tools/search.rs", "crates/kungfu-mcp/src/tools/context.rs", "crates/kungfu-mcp/src/scope.rs", "CLAUDE.md"]
related_symbols = ["graph_response", "apply_scope", "embeddings_build"]
pinned = false
status = "active"
created_at = "2026-06-11T12:51:24.818918+00:00"
updated_at = "2026-06-11T12:51:24.818918+00:00"
+++

Эпик 6 (1ksso8 + jctlg8), v2.5.17. Финал must-have плана pbkq8h — все 6 эпиков закрыты.

SLIMMING (40→38): сняты MCP-тулы get_symbol, find_related_symbols, diff_context (покрыты explore_symbol/investigate; КЛЮЧЕВОЕ: методы ядра и CLI-команды остались — снята только MCP-экспозиция). Удалены делегаты из lib.rs #[tool_router] + тела из tools/search.rs, tools/context.rs. Упрощён делегат embeddings_build: был async+spawn_blocking — после эпика 4 тело само спавнит поток, двойная обёртка убрана; описание актуализировано (background + poll status).

PROVENANCE:
- callers/callees: ЕДИНАЯ форма {status:"ok", name, results, provenance} (раньше непустой = голый массив, пустой = объект — несимметрия этапа 0). graph_response() helper. provenance-текст: рёбра только при однозначном резолве, ambiguous пропускаются, не угадываются. apply_scope научен фильтровать "results" (раньше только "items") — иначе scope перестал бы работать для новой формы.
- Пустые find_symbol/search_text → {results:[], hint} с направлением на semantic_search/search_text вместо голого [].
- Инвариант «Retrieval honesty» закреплён секцией в CLAUDE.md проекта: каждый ответ декларирует режим/усечение/отсутствующий слой/следующий шаг; молчаливый [] или немаркированное усечение = баг.

ДОГФУД ЧЕРЕЗ ЖИВОЙ MCP СЕССИИ (после реконнекта, v2.5.17): verify_change на реальном диффе эпика (15 символов, risk HIGH, 31 affected с note "showing 20 of 31", контракты, в reasons смешанные calls+imports рёбра); edit_context на функции, написанной 30 минут назад (verbatim тело + точные callers — индекс свежий); callers новой формы; semantic_search mode=vector coverage 923/935 с честной note → reindex через MCP → фоновый sync → 935/935 "ready". Снятые тулы исчезли из харнесса, новые появились.

ГРАНИЦА ДИЗАЙНА (задокументированная): авто-sync эмбеддингов вешается только на MCP-пути (reindex-тул + lazy-реиндекс в service()); CLI-реиндекс вектора не синкает — отставание честно декларируется coverage/note в semantic_search и закрывается следующим MCP-reindex.
