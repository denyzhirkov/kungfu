{
  "id": "case-022",
  "category": "concept-recall",
  "project": "redis",
  "task": "Find the eviction policy code that frees keys under memory pressure",
  "input": {
    "query": "what happens when the memory limit is reached and keys must be freed",
    "command": "ask-context",
    "budget": "small"
  },
  "expected": {
    "must_include": [
      "src/evict.c"
    ],
    "should_include": [
      "performEvictions"
    ],
    "should_not_include": [
      "README.md"
    ],
    "max_files": 8,
    "max_bytes": 5000
  }
}
